type Movie {
    id: Int!
    title: String!
    rating: Float!
    summary: String!
    language: String!
    medium_cover_image: String!
}

type Test {
    name: String!
}

type Query {
    movies1(limit: Int, rating: Float): [Movie]!
    test: Test
}
